---
phase: 02-static-sections
plan: 06
type: execute
wave: 6
depends_on: [02-05]
files_modified:
  - src/components/ui/ProjectCard.astro
  - src/components/sections/ExperienceSection.astro
autonomous: true
requirements: [PROJ-03]
gap_closure: true

must_haves:
  truths:
    - "Hovering a project card lifts it visually (translateY up + box-shadow increase)"
    - "The lift transition is smooth (no jump)"
    - "ExperienceSection.astro has no unused imports"
  artifacts:
    - path: "src/components/ui/ProjectCard.astro"
      provides: "Hover lift animation on .project-card"
      contains: "transform: translateY"
    - path: "src/components/sections/ExperienceSection.astro"
      provides: "Clean import block — Panel removed"
  key_links:
    - from: "src/components/ui/ProjectCard.astro"
      to: ":global(.project-card:hover)"
      via: "CSS :global selector"
      pattern: "project-card:hover"
---

<objective>
Close the two gaps identified in 02-VERIFICATION.md:
1. PROJ-03 (blocker): Add hover lift/shadow animation to ProjectCard.astro
2. Warning: Remove unused Panel import from ExperienceSection.astro

Purpose: Satisfy PROJ-03 so Phase 2 achieves 26/26 must-haves. Clean up the dead import warning.
Output: ProjectCard.astro with hover animation; ExperienceSection.astro with clean imports.
</objective>

<execution_context>
@/Users/jack.winter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jack.winter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-static-sections/02-05-SUMMARY.md

<interfaces>
<!-- Current ProjectCard.astro :global block (lines 106–108) — executor extends this: -->
```css
:global(.project-card) {
  cursor: pointer;
}
```

<!-- ExperienceSection.astro import block (line 2) — executor removes this line: -->
```astro
import Panel from '../Panel.astro';
```
<!-- Panel is imported but never rendered in ExperienceSection.astro template -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add hover lift animation to ProjectCard.astro</name>
  <files>src/components/ui/ProjectCard.astro</files>
  <action>
    Extend the existing `:global(.project-card)` rule in the `<style>` block to add a transition property, then add a `:global(.project-card:hover)` rule beneath it.

    The existing rule is at lines 106–108:
    ```css
    :global(.project-card) {
      cursor: pointer;
    }
    ```

    Update it to:
    ```css
    :global(.project-card) {
      cursor: pointer;
      transition: transform 0.15s ease, box-shadow 0.15s ease;
    }

    :global(.project-card:hover) {
      transform: translateY(-4px);
      box-shadow: 8px 12px 0 var(--color-shadow);
    }
    ```

    The `box-shadow` value on hover should be slightly larger/more offset than the Panel's base shadow (the Panel component uses a hard drop-shadow established by the design system). Using `8px 12px 0 var(--color-shadow)` lifts the card's shadow to match the translateY movement.

    Note: The CONTEXT.md Phase 2 discussion deferred hover animation to Phase 3, but REQUIREMENTS.md marks PROJ-03 as Phase 2 Complete and the VERIFICATION.md flags it as a blocker gap. This gap closure plan resolves that contradiction by implementing the animation in Phase 2 as the requirements specify. No Phase 3 work is blocked — Phase 3 can replace this with a JS-driven or scroll-triggered version if needed.

    Do NOT touch any other styles or markup in ProjectCard.astro.
  </action>
  <verify>
    <automated>grep -n "translateY\|transition.*transform\|project-card:hover" /Users/jack.winter/dev/sandbox/winterbot/src/components/ui/ProjectCard.astro</automated>
  </verify>
  <done>
    Three matches found: the `transition` property on `.project-card`, the `:global(.project-card:hover)` selector, and `transform: translateY(-4px)` inside it.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove unused Panel import from ExperienceSection.astro</name>
  <files>src/components/sections/ExperienceSection.astro</files>
  <action>
    Delete line 2 of ExperienceSection.astro:
    ```astro
    import Panel from '../Panel.astro';
    ```

    This import is unused — ExperienceSection never renders `<Panel>` in its template. The entry-content div uses manual border/shadow CSS instead (a deliberate decision to allow `.is-current` border-color override without `:global`). The import is simply a leftover from initial scaffolding.

    After removal the frontmatter block will contain only the `entries` array constant with no imports. Verify the template is unchanged — no `<Panel>` should be added.

    Do NOT modify any other part of ExperienceSection.astro.
  </action>
  <verify>
    <automated>grep -n "import Panel" /Users/jack.winter/dev/sandbox/winterbot/src/components/sections/ExperienceSection.astro; echo "exit: $?"</automated>
  </verify>
  <done>
    grep returns no matches (exit code 1 or empty output) — the import line is gone.
  </done>
</task>

</tasks>

<verification>
After both tasks:

1. `grep -n "translateY\|project-card:hover" src/components/ui/ProjectCard.astro` — must return 2+ lines
2. `grep -n "import Panel" src/components/sections/ExperienceSection.astro` — must return nothing
3. `npm run build` in the project root — must complete with 0 errors (confirms no broken imports)
</verification>

<success_criteria>
- PROJ-03 gap closed: ProjectCard.astro has `transform: translateY(-4px)`, `box-shadow` lift, and `transition` on hover
- Dead import gap closed: ExperienceSection.astro has no Panel import
- Build passes: `npm run build` exits 0
- No other files modified
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-sections/02-06-SUMMARY.md` following the summary template.
</output>
