---
phase: 02-static-sections
plan: 05
type: execute
wave: 3
depends_on: [02-02, 02-03, 02-04]
files_modified:
  - src/pages/index.astro
autonomous: false
requirements: [NAV-01, NAV-02, HERO-01, HERO-02, HERO-03, HERO-04, ABOUT-01, ABOUT-02, EXP-01, EXP-02, EXP-03, PROJ-01, PROJ-02, PROJ-03, PROJ-04, SKILL-01, SKILL-02, SKILL-03, SKILL-04, CONT-01, CONT-02, CONT-03, A11Y-01, A11Y-02, A11Y-03]

must_haves:
  truths:
    - "Visiting / renders all six sections in order: Hero, About, Experience, Projects, Skills, Contact"
    - "All 21 automated Playwright tests pass green"
    - "Full test suite (npx playwright test --project=chromium) exits 0"
    - "Site is usable on 320px mobile viewport — no horizontal scroll, nav hidden, content fills width"
    - "Visitor can keyboard-tab through all interactive elements with visible focus rings"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Single-page site importing all 6 section components in correct order"
      contains: "HeroSection, AboutSection, ExperienceSection, ProjectsSection, SkillsSection, ContactSection"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/sections/*.astro"
      via: "6 component imports"
      pattern: "import.*Section"
    - from: "section#hero"
      to: "nav[aria-label='Site sections']"
      via: "matching id attributes to nav hrefs"
      pattern: "id=\"hero\""
---

<objective>
Wire all six section components into index.astro, then run the full automated test suite and do a visual checkpoint to confirm the page renders correctly end-to-end.

Purpose: This is the integration and sign-off plan — all prior Wave 2 components exist but aren't assembled yet. This plan plugs them in and gets human eyes on the result.
Output: Updated src/pages/index.astro; all tests green; human-approved visual checkpoint.
</objective>

<execution_context>
@/Users/jack.winter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jack.winter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-static-sections/02-CONTEXT.md
@.planning/phases/02-static-sections/02-02-SUMMARY.md
@.planning/phases/02-static-sections/02-03-SUMMARY.md
@.planning/phases/02-static-sections/02-04-SUMMARY.md
</context>

<interfaces>
<!-- All section components created in Wave 2. Verify exact file paths from SUMMARYs. -->

Wave 2 components (verify paths exist before importing):
- src/components/sections/HeroSection.astro
- src/components/sections/AboutSection.astro
- src/components/sections/ExperienceSection.astro
- src/components/sections/ProjectsSection.astro
- src/components/sections/SkillsSection.astro
- src/components/sections/ContactSection.astro

Section IDs (must match nav hrefs exactly):
- #hero, #about, #experience, #projects, #skills, #contact

Layout.astro already renders JourneyNav (added in Plan 02-02). index.astro
only needs to provide the section content — no need to re-import Nav.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire index.astro — import and render all six sections</name>
  <files>src/pages/index.astro</files>
  <action>
Replace the current placeholder content in `src/pages/index.astro` with imports and renders of all six section components.

The resulting file should look like:
```astro
---
import Layout from '../layouts/Layout.astro';
import HeroSection from '../components/sections/HeroSection.astro';
import AboutSection from '../components/sections/AboutSection.astro';
import ExperienceSection from '../components/sections/ExperienceSection.astro';
import ProjectsSection from '../components/sections/ProjectsSection.astro';
import SkillsSection from '../components/sections/SkillsSection.astro';
import ContactSection from '../components/sections/ContactSection.astro';
---

<Layout title="WinterBot — Frontend Engineer Portfolio">
  <HeroSection />
  <AboutSection />
  <ExperienceSection />
  <ProjectsSection />
  <SkillsSection />
  <ContactSection />
</Layout>
```

After writing this file, run the full automated test suite to confirm all 21 stubs now pass:
```
npx playwright test --project=chromium tests/static-sections.spec.ts
```

If any tests fail, diagnose and fix. Common causes:
- Section ID mismatch (`id="experience-section"` vs expected `id="experience"`) — update the section component
- Missing class on element (`.projects-grid` vs `.project-grid`) — update the section component
- astro-icon rendering an outer wrapper that changes the svg selector — adjust the A11Y-03 test selector if needed

Once all 21 static-sections tests pass, run the design-system tests too to confirm nothing regressed:
```
npx playwright test --project=chromium tests/design-system.spec.ts
```
  </action>
  <verify>
    <automated>npx playwright test --project=chromium tests/static-sections.spec.ts 2>&1 | tail -10</automated>
  </verify>
  <done>src/pages/index.astro imports and renders all 6 sections; all 21 Playwright static-sections tests pass green; design-system tests still pass (no regressions).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
The complete Phase 2 site is assembled: fixed left sidebar nav with diamond journey markers, Hero trainer card, About origin story, Experience quest log timeline, Projects dex-entry grid, Skills inventory slots with category tabs and CSS tooltips, Contact "Rest Area / Save Point" section. All automated tests pass.
  </what-built>
  <how-to-verify>
Run `npm run dev` and visit http://localhost:4321, then verify:

1. **Navigation sidebar** — visible on left edge; 6 items (START, ORIGIN, QUEST LOG, DEX, INVENTORY, SAVE POINT); dashed vertical line connecting markers; clicking each item scrolls smoothly to the correct section
2. **Hero section** — trainer card visible with portrait placeholder, Name/Class/Level/Region stats; pixel level bar visible; headline and tagline outside the card; "View Projects" and "Contact" CTA buttons present
3. **About section** — Panel-wrapped content visible with backstory copy
4. **Experience section** — vertical timeline with 3 entries; first entry has an ACTIVE badge and accent-colored marker; timeline has a connector line
5. **Projects section** — 3-column grid (on desktop) with 3 project cards; each card shows dex number (#001 style), project name, tech tag pills, GitHub link
6. **Skills section** — tab buttons (ALL / FRONTEND / BACKEND / TOOLS) render above the inventory grid; skill slots have icons and labels; hovering a slot shows the "Expert • 5+ years" style tooltip
7. **Contact section** — heading reads "Rest Area / Save Point"; email, GitHub, LinkedIn links present; panel has visually warmer styling than other sections
8. **Mobile check** — resize browser to 320px width: nav sidebar hidden, content fills width, no horizontal scroll, all text readable
9. **Keyboard check** — tab through the page: focus rings visible on nav links, CTA buttons, card links, contact links, skill slots
10. **Theme toggle** — fire/leaf switch still works; both themes render correctly across all sections
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
Phase 2 is complete when:
1. `npx playwright test --project=chromium` — full suite green (both spec files)
2. Human visual checkpoint approved
3. All 25 Phase 2 requirements addressed (21 automated + 4 manual-visual)
</verification>

<success_criteria>
- src/pages/index.astro renders all 6 sections in order
- All 21 automated tests in tests/static-sections.spec.ts pass green
- design-system.spec.ts still passing (no regressions)
- Human checkpoint: visual review approved
- Mobile 320px: no horizontal scroll, nav hidden, content usable
- Keyboard nav: all interactive elements reachable with visible focus rings
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-sections/02-05-SUMMARY.md` following the summary template.
</output>
