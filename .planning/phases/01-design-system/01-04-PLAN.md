---
phase: 01-design-system
plan: 04
type: execute
wave: 3
depends_on: [01-02, 01-03]
files_modified: []
autonomous: false
requirements: [DS-07]

user_setup:
  - service: vercel
    why: "Continuous deployment from git push — DS-07 requires zero manual deploy steps after initial setup"
    env_vars: []
    dashboard_config:
      - task: "Connect GitHub repository to Vercel"
        location: "vercel.com → Add New Project → Import Git Repository → select winterbot repo"
        note: "Vercel auto-detects Astro. Accept defaults: build command `npm run build`, output `dist/`. No vercel.json needed."
      - task: "Verify deployment URL after first push"
        location: "Vercel Dashboard → project → Deployments tab"

must_haves:
  truths:
    - "Pushing a commit to main triggers an automatic Vercel build with no manual intervention"
    - "The Vercel deployment URL returns HTTP 200 and renders the site correctly"
    - "All 6 smoke tests pass green before deployment"
  artifacts:
    - path: ".vercel/project.json"
      provides: "Vercel project link (created by Vercel CLI or dashboard import)"
  key_links:
    - from: "git push origin main"
      to: "Vercel production deployment"
      via: "GitHub integration (set up in Vercel dashboard)"
      pattern: "automatic"
---

<objective>
Initialize the git repository, push to GitHub, connect to Vercel for automatic deployments, and verify Phase 1 is complete. This plan has one checkpoint: the user connects the repo to Vercel (a one-time manual action in the Vercel dashboard that has no CLI equivalent for the initial GitHub integration setup).

Purpose: DS-07 requires automatic deployment from git push. That pipeline must be established and verified before Phase 1 closes.
Output: Site live on Vercel, deploying automatically on every push to main.
</objective>

<execution_context>
@/Users/jack.winter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jack.winter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-design-system/01-02-SUMMARY.md
@.planning/phases/01-design-system/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize git and verify full test suite</name>
  <files>.</files>
  <action>
    Run the full Playwright test suite to confirm Plans 02 and 03 are complete:
    ```
    npx playwright test
    ```
    All 6 tests (DS-01 through DS-06) must pass green before proceeding. If any are red, stop and surface the failure — do not proceed to deployment with failing tests.

    Then initialize git and make the initial commit:
    ```
    git init
    git add .
    git commit -m "feat(01-design-system): establish design system foundation

    - Astro 5 + Tailwind v4 via @tailwindcss/vite
    - Fire/Leaf dual-theme token system with FOUC prevention
    - Panel primitive with inner highlight ring
    - ThemeToggle client island
    - Playwright smoke tests DS-01 through DS-06"
    ```

    If a GitHub repo for winterbot already exists, add the remote and push:
    ```
    git remote add origin https://github.com/<username>/winterbot.git
    git branch -M main
    git push -u origin main
    ```

    If no GitHub repo exists yet, create one via the GitHub CLI:
    ```
    gh repo create winterbot --public --source=. --remote=origin --push
    ```
    (Requires `gh` CLI authenticated. If not installed/authenticated, surface this as a blocker for the user to resolve before the checkpoint.)
  </action>
  <verify>
    <automated>npx playwright test 2>&1 | tail -10</automated>
  </verify>
  <done>
    All 6 Playwright smoke tests pass green. Git repo initialized with initial commit. Code pushed to GitHub main branch.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint: Connect repo to Vercel for automatic deployments</name>
  <files></files>
  <action>Human connects the winterbot GitHub repo to Vercel via the Vercel dashboard import flow. Vercel auto-detects Astro — no configuration changes needed. User deploys and confirms the production URL works.</action>
  <verify>
    <automated>MISSING — DS-07 deployment check is manual-only (requires external Vercel account and git push to trigger CI; see VALIDATION.md)</automated>
  </verify>
  <done>Vercel deployment URL confirmed live and returning 200. Automatic deploy pipeline established (future git push to main triggers Vercel build automatically).</done>
  <what-built>Git repo pushed to GitHub with full Phase 1 design system implementation. All 6 smoke tests passing.</what-built>
  <how-to-verify>
    1. Go to https://vercel.com and sign in (create account if needed — free tier is sufficient)
    2. Click "Add New..." → "Project"
    3. Under "Import Git Repository", find and select the `winterbot` repo
    4. Vercel will auto-detect Astro — you should see:
       - Framework Preset: Astro
       - Build Command: `npm run build`
       - Output Directory: `dist`
    5. Click "Deploy" — accept all defaults, no changes needed
    6. Wait for the first deployment to complete (1-2 minutes)
    7. Click "Visit" to open the production URL
    8. Verify the site loads, Fire theme is active, and the theme toggle button is visible
    9. Paste the production URL below

    After this one-time setup, all future `git push origin main` commands will automatically trigger Vercel deployments.
  </how-to-verify>
  <resume-signal>Paste the Vercel deployment URL (e.g., https://winterbot.vercel.app) and type "deployed"</resume-signal>
</task>

</tasks>

<verification>
After the checkpoint resolves:
- Visit the Vercel URL — site renders with Fire theme background
- Click the theme toggle — world background shifts to Leaf mode, persists on refresh
- Panel element visible with chunky border and inner highlight ring
- Make a trivial commit (e.g., add a comment to index.astro), push to main — verify Vercel auto-deploys within 2 minutes
</verification>

<success_criteria>
- All 6 Playwright smoke tests pass green locally
- Vercel production URL returns 200 and renders the site correctly
- Pushing to main triggers automatic Vercel deployment (DS-07 satisfied)
- Phase 1 design system foundation complete
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system/01-04-SUMMARY.md`
</output>
